#
# ===============================
# Manually trimmed - the daily schedule is disabled (workflow_dispatch still
# works if you ever want to run it by hand). This would otherwise silently
# undo the customizations in setup.yml (removed "Generate site" and
# "GitHub Pages Deploy" - not used, since the public status page lives on
# stream.rossmackenzie.co.uk/status instead) and the deleted site.yml.
# ===============================
#
# 🔼 Upptime @v1.43.13
# GitHub-powered open-source uptime monitor and status page by Anand Chowdhary

# * Source: https://github.com/upptime/upptime
# * Docs and more: https://upptime.js.org
# * More by Anand Chowdhary: https://anandchowdhary.com


name: Update Template CI
on:
  repository_dispatch:
    types: [update_template]
  workflow_dispatch:
concurrency:
  group: ${{ github.repository }}-${{ github.head_ref || github.ref_name }}-upptime
  cancel-in-progress: false
jobs:
  release:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          token: ${{ secrets.GH_PAT || github.token }}
      - name: Update template
        uses: upptime/uptime-monitor@master
        with:
          command: "update-template"
        env:
          GH_PAT: ${{ secrets.GH_PAT || github.token }}
